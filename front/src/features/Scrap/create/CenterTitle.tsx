import { TitleDiv, TitleP } from '../scrapStyledComponent';

const CenterTitle: React.FC = () => {
  return (
    <TitleDiv>
      <TitleP>스크랩 작성</TitleP>
    </TitleDiv>
  );
};

export default CenterTitle;
